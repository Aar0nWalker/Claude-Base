<!-- BEGIN:nextjs-agent-rules -->
# This is NOT the Next.js you know

This version (Next.js 16) has breaking changes — APIs, conventions, and file structure may differ
from your training data. Read the relevant guide in `node_modules/next/dist/docs/` before writing
code, and heed deprecation notices.
<!-- END:nextjs-agent-rules -->

# Frontend Notes

Follow the root project instructions in `../../AGENTS.md`. UI conventions: CSS-variable design tokens
(`app/globals.css`), no Tailwind, `lucide-react` icons, hand-rolled providers (`components/providers.tsx`).
